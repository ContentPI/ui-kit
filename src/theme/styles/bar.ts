import { CustomTheme } from '../../types'
import { Yellow } from '../color'

const customTheme: CustomTheme = {
  palette: {
    primary: {
      common: {
        main: Yellow.V050,
        dark: Yellow.V150,
        light: Yellow.V100,
        contrastText: Yellow.V200,
      },
    },
  },
}

export default customTheme
